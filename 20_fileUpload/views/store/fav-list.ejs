<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
  <title>My Favorites</title>
</head>

<body class="bg-gray-50">

  <%- include('../partials/nav') %>

  <main class="max-w-[1200px] mx-auto px-6 py-10">
    <h1 class="text-3xl font-bold text-gray-900 mb-8 border-b pb-4">Favourites Homes</h1>

    <% if (favData.length > 0) { %>
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">

      <% favData.forEach(home => { %>
      <div class="group bg-white rounded-2xl overflow-hidden shadow-sm hover:shadow-xl transition-all duration-300 border border-gray-100 flex flex-col h-full">

        <div class="relative aspect-square">
          <img src="<%= home.image %>" class="w-full h-full object-cover group-hover:scale-105 transition duration-500" alt="<%= home.title %>">

          <div class="absolute top-3 right-3">
            <form action="/favorites/remove/<%= home._id %>" method="POST">
              <button type="submit" class="bg-white/90 p-2 rounded-full shadow-md hover:bg-red-50 transition">

                <svg xmlns="http://www.w3.org/2000/svg" fill="#FF385C" viewBox="0 0 24 24" stroke="#FF385C" class="w-5 h-5">
                  <path d="M21 8.25c0-2.485-2.099-4.5-4.688-4.5-1.935 0-3.597 1.126-4.312 2.733-.715-1.607-2.377-2.733-4.313-2.733C5.1 3.75 3 5.765 3 8.25c0 7.22 9 12 9 12s9-4.78 9-12Z" />
                </svg>

              </button>
            </form>
          </div>

        </div>

        <div class="p-4 flex flex-col flex-grow">
          <div class="flex justify-between items-start mb-1">
            <h3 class="font-bold text-gray-900 truncate flex-1 text-lg"><%= home.title %></h3>
            <span class="text-[10px] bg-gray-100 px-2 py-1 rounded text-gray-600 font-bold uppercase ml-2">
              <%= home.type %>
            </span>
          </div>

          <p class="text-gray-500 text-sm mb-4"><%= home.location %></p>

          <div class="mt-auto pt-4 border-t flex items-center justify-between">
            <div>
              <span class="font-bold text-gray-900 text-lg">$<%= home.price %></span>
              <span class="text-gray-500 text-sm">/night</span>
            </div>


          </div>
        </div>
      </div>
      <% }) %>

    </div>
    <% } else { %>
    <div class="text-center py-24 bg-white rounded-3xl border border-dashed border-gray-300">
      <p class="text-4xl mb-4">🏠</p>
      <h2 class="text-xl font-semibold text-gray-800">Your wishlist is empty</h2>
      <p class="text-gray-500 mt-1">Start exploring homes to save your favorites.</p>
      <a href="/" class="inline-block mt-6 underline font-bold text-gray-900">Go explore</a>
    </div>
    <% } %>
  </main>

</body>

</html>