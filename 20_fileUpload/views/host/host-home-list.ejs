<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
  <title>Airbnb Clone - Manage My Listings</title>
</head>

<body class="bg-white">

  <%- include('../partials/nav') %>

  <main class="max-w-[1200px] mx-auto px-8 py-10">
    <div class="mb-8">
        <h1 class="text-3xl font-bold text-gray-900">Your Listings</h1>
        <p class="text-gray-500">Manage your properties and keep them up to date.</p>
    </div>

    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">

      <% HomeData.forEach(home => { %>
      <div class="group flex flex-col h-full border rounded-xl overflow-hidden shadow-sm p-2">
        <div class="relative overflow-hidden rounded-lg aspect-square">
          <img class="object-cover w-full h-full group-hover:scale-105 transition duration-300" src="<%= home.image %>" alt="<%= home.title %>">
          
          <div class="absolute top-2 left-2">
            <span class="text-[10px] bg-white/90 backdrop-blur px-2 py-1 rounded text-gray-900 uppercase font-bold tracking-wider">
                <%= home.type %>
            </span>
          </div>
        </div>

        <div class="mt-3 px-1 flex-grow">
          <h3 class="font-bold text-gray-900 capitalize text-base truncate"><%= home.title %></h3>
          <p class="text-gray-500 text-sm truncate"><%= home.location %></p>
          <p class="mt-1 font-bold text-gray-900">$<%= home.price %> <span class="font-normal text-gray-500 text-sm">/night</span></p>
        </div>

        <div class="mt-4 flex gap-2">
          <a href="/host/edit-home/<%= home._id %>?editing=true" class="flex-1">
            <button class="w-full border border-gray-300 text-gray-700 py-2 rounded-lg font-semibold hover:bg-gray-50 transition-colors duration-200">
              Edit
            </button>
          </a>

          <form action="/host/delete-home/<%= home._id %>" method="POST" class="flex-1">
            <button type="submit" class="w-full bg-red-50 text-red-600 border border-red-100 py-2 rounded-lg font-semibold hover:bg-red-600 hover:text-white transition-all duration-200">
              Delete
            </button>
          </form>
        </div>
      </div>
      <% }) %>

      <% if(HomeData.length === 0) { %>
      <div class="col-span-full text-center py-20 bg-gray-50 rounded-2xl border-2 border-dashed border-gray-200">
        <h2 class="text-xl text-gray-500 font-medium">You haven't added any homes yet.</h2>
        <a href="/AddHome" class="mt-4 inline-block text-[#ff385c] font-bold hover:underline">Start Hosting Now</a>
      </div>
      <% } %>

    </div>
  </main>

</body>
</html>