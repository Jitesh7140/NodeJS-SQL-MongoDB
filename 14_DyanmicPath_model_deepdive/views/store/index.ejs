<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
  <title>Airbnb Clone - Home</title>
</head>

<body class="bg-white">

  <%- include('../partials/nav') %>

  <main class="max-w-[1200px] mx-auto px-8 py-10">
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
 
      <% HomeData.forEach(home => { %>
      <div class="group cursor-pointer flex flex-col h-full">
        <div class="relative overflow-hidden rounded-xl aspect-square">
          <img class="object-cover w-full h-full group-hover:scale-105 transition duration-300" src="<%= home.image %>" alt="<%= home.title %>">

          <div class="absolute top-3 right-3">
            <svg xmlns="http://www.w3.org/2000/svg" fill="rgba(0,0,0,0.5)" viewBox="0 0 24 24" stroke-width="1.5" stroke="white" class="w-6 h-6">
              <path stroke-linecap="round" stroke-linejoin="round" d="M21 8.25c0-2.485-2.099-4.5-4.688-4.5-1.935 0-3.597 1.126-4.312 2.733-.715-1.607-2.377-2.733-4.313-2.733C5.1 3.75 3 5.765 3 8.25c0 7.22 9 12 9 12s9-4.78 9-12Z" />
            </svg>
          </div>
        </div>

        <div class="mt-3 flex-grow">
          <div class="flex justify-between items-start">
            <h3 class="font-bold text-gray-900 capitalize text-base"><%= home.title %></h3>
            <span class="text-[10px] bg-gray-100 px-2 py-1 rounded text-gray-600 uppercase font-bold tracking-wider">
              <%= home.type %>
            </span>
          </div>
          <p class="text-gray-500 text-sm"><%= home.location %></p>
          <p class="mt-1"><span class="font-bold text-gray-900">$<%= home.price %></span> <span class="text-gray-500 text-sm">/night</span></p>
        </div>

        
          <button class="w-full bg-[#ff385c] text-white py-2.5 rounded-lg font-semibold hover:bg-[#e31c5f] transition-colors duration-200 shadow-sm">
            Book Now
          </button>
        
      </div>
      <% }) %>

      <% if(HomeData.length === 0) { %>
      <div class="col-span-full text-center py-20">
        <h2 class="text-2xl text-gray-400 font-light">No homes listed yet.</h2>
      </div>
      <% } %>

    </div>
  </main>

</body>

</html>