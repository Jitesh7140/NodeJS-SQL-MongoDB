<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Airbnb - Add Your Home</title>
</head>
<body class="bg-gray-50 font-sans">

      <%- include('../partials/nav') %>

    <div class="max-w-6xl mx-auto flex flex-wrap lg:flex-nowrap items-center py-10 px-6 gap-12">
        
        <div class="w-full lg:w-1/2">
            <h1 class="text-4xl md:text-5xl font-semibold leading-tight text-gray-900">
                It’s easy to get started <br> on Airbnb.
            </h1>
            <p class="text-xl text-gray-600 mt-6 italic">"Turn your extra space into your next adventure."</p>
            
            <div class="mt-10 space-y-8">
                <div class="flex gap-4">
                    <div class="text-xl font-bold border-b-2 border-[#ff385c] h-8">01</div>
                    <div>
                        <h4 class="font-bold text-lg">Tell us about your place</h4>
                        <p class="text-gray-500">Share some basic info, like where it is and how many guests can stay.</p>
                    </div>
                </div>
                <div class="flex gap-4">
                    <div class="text-xl font-bold border-b-2 border-[#ff385c] h-8">02</div>
                    <div>
                        <h4 class="font-bold text-lg">Make it stand out</h4>
                        <p class="text-gray-500">Add 5 or more photos plus a title and description.</p>
                    </div>
                </div>
            </div>
        </div>

        <div class="w-full lg:w-1/2 bg-white p-8 rounded-2xl shadow-2xl border border-gray-100">
            <h2 class="text-2xl font-semibold mb-6">Home Details</h2>
            
          <form action="<%= editing ? '/host/edit-home' : '/host/AddHome' %>" method="POST" class="space-y-5">
                
    <% if(editing){ %>
        <input type="hidden" name="id" value="<%= home._id %>">
    <% } %>

    <div>
        <label class="block text-sm font-semibold text-gray-700 mb-1">
            Where is your place Name?
        </label>
        <input type="text" 
            name="title" 
            required 
            placeholder="e.g. Luxury Beach Villa"
            value="<%= editing ? home.title : '' %>"
            class="w-full p-3 border rounded-lg focus:ring-2 focus:ring-[#ff385c] focus:outline-none border-gray-300">
    </div>

    <div class="grid grid-cols-2 gap-4">
        <div>
            <label class="block text-sm font-semibold text-gray-700 mb-1">
                Property Type
            </label>
            <select name="type" 
                class="w-full p-3 border rounded-lg focus:outline-none border-gray-300 bg-white">

                <option value="House" 
                    <%= editing && home.type === "House" ? "selected" : "" %>>
                    House
                </option>

                <option value="Apartment" 
                    <%= editing && home.type === "Apartment" ? "selected" : "" %>>
                    Apartment
                </option>

                <option value="Villa" 
                    <%= editing && home.type === "Villa" ? "selected" : "" %>>
                    Villa
                </option>

                <option value="Cabin" 
                    <%= editing && home.type === "Cabin" ? "selected" : "" %>>
                    Cabin
                </option>

            </select>
        </div>

        <div>
            <label class="block text-sm font-semibold text-gray-700 mb-1">
                Price per night
            </label>
            <input type="number" 
                name="price" 
                required 
                placeholder="$100"
                value="<%= editing ? home.price : '' %>"
                class="w-full p-3 border rounded-lg focus:outline-none border-gray-300">
        </div>
    </div>

    <div>
        <label class="block text-sm font-semibold text-gray-700 mb-1">
            Address
        </label>
        <input type="text" 
            name="location" 
            required 
            placeholder="City, Country"
            value="<%= editing ? home.location : '' %>"
            class="w-full p-3 border rounded-lg focus:ring-2 focus:ring-[#ff385c] focus:outline-none border-gray-300">
    </div>

    <div>
        <label class="block text-sm font-semibold text-gray-700 mb-1">
            Image URL
        </label>
        <input type="url" 
            name="image" 
            required 
            placeholder="https://images.unsplash.com/..."
            value="<%= editing ? home.image : '' %>"
            class="w-full p-3 border rounded-lg focus:ring-2 focus:ring-[#ff385c] focus:outline-none border-gray-300">
    </div>

    <button type="submit" 
        class="w-full bg-[#ff385c] text-white font-bold py-4 rounded-lg hover:bg-[#e31c5f] transition shadow-md mt-4 text-lg">
        <%= editing ? "Update Home" : "Finish & List your home" %>
    </button>

</form>

        </div>

    </div>

</body>
</html>